# Comma-separated list of frontend origins to allow (in addition to localhost defaults).
# Set this to your deployed frontend URL so CORS works in production.
ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-app.railway.app
